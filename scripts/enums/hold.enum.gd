@export enum GripTypeEnum {
    JUG = "jug" # bucket like hold
    CRIMP = "crimp" # small finger hold
    SLOPER = "sloper" # rounder hold, requires friction
    PINCH = "pinch" # pinch what else
    POCKET = "pocket" # 1-3 finger holes
}