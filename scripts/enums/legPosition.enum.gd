@export enum LegPositionEnum {
    STANDING = "standing" # neutral stance, no stamina loss
    HANGING = "hanging" # arm stress, stamina loss
    FLAGGING = "flagging" # leg extended, minimal stamina loss
    STEMMING = "stemming" # both legs pressure on opposite sides, moderate stamina loss
    HEEL_HOOK = "hook" # hooking heel for stability, minimal stamina loss
}